# TRAIL — landing page

The Next.js landing page for TRAIL, part of the [root monorepo](../README.md). Still the `create-next-app` scaffold: the hero, embedded demo video, and "how it works" section from the product spec are Phase 5 and haven't been built yet.

```sh
pnpm dev    # http://localhost:3000
```

The page lives in `app/page.tsx`. Design tokens come from `@trail/tokens`.
